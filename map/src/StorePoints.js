//class StorePoints {

    //init(pointData) {
       // this.points = pointData;
        //this.subscribers = [];
  //  }

   // changePoints(pointIDs) {
        //this.points[pointIDs] = { x: 0, y: 0};

       // this.subscribers.forEach((subscribers) => { subscribers.pointsUpdated();
       // })
   // }

   // register(Object) {
        //this.subscribers.push(object);
       // console.log(this.subscribers);
  //  }
//}

//export default new StorePoints();